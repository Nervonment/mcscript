fn get_arr(length: int, a: int) -> Array<Array<Array<int>>> {
    let arr = new Array(length, new Array(length, new Array(length, fib::fib(a))));
    return arr;
}

fn main() -> Array<Array<Array<int>>> {
    return get_arr(2, 11);
}