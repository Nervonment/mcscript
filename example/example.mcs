fn main() -> Array<Array<int>> {
    let a = 0;
    a = 114514;

    let arr = new Array(2, 1919810);
    arr[0] = a;

    let arr1 = new Array(2, new Array(2, fib::fib(20)));
    arr1[0] = arr; 
    return arr1;
}