fn fib(n) {
    if n < 2 {
        return 1;
    }
    let x = n;
    x = x - 1;
    return fib(x) + fib(n - 2);
}

fn sum(n) {
    if n == 0 {
        return 0;
    }
    return n + sum(n - 1);
}

fn main() {
    let a = sum(100);
    a = a - 5050;
    a = a + fib(10);
    return a;
}